export const AUTH_QUERY_KEY = ["auth", "me"] as const;
export const SCHEDULED_EMAILS_QUERY_KEY = ["emails", "scheduled"] as const;
export const SENT_EMAILS_QUERY_KEY = ["emails", "sent"] as const;
export const SENDERS_QUERY_KEY = ["senders"] as const;
